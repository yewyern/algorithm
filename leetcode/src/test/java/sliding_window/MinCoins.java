package sliding_window;

/**
 * arr是货币数组，其中的值都是正数。再给定一个正数aim。
 * 每个值都认为是一张货币
 * 返回组成aim的最少货币数
 * 注意:
 * 因为是求最少货币数，所以每一张货币认为是相同或者不同就不重要了
 *
 * @author zhou.xu
 * @since 2023/6/18 18:35
 */
public class MinCoins {

    public static int minCoins(int[] arr, int aim) {
        return 0;
    }
}
